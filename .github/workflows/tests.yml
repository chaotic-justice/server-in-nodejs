name: Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  DATABASE_URL: postgres://postgres:passw00d@localhost:5432/nest-db
  PORT: 3000
  JWT_ACCESS_SECRET: rasp3028*Q()berry
  JWT_REFRESH_SECRET: rasp3028*Qberry

jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/build
      - name: Run unit tests
        run: pnpm test:unit
  integration-tests:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - uses: ./.github/actions/build
        - uses: ./.github/actions/docker-compose
        - name: Run tests
          run: pnpm run test:backend:int